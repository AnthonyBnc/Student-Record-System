#include <stdio.h>
#include "menu.h"

void show_menu(void) {
    puts("\n===== Student Record System =====");
    puts("1. Add student");
    puts("2. List students");
    puts("3. Search by ID");
    puts("4. Search by name");
    puts("5. Update by ID");
    puts("6. Delete by ID");
    puts("7. Sort");
    puts("8. Save now");
    puts("9. Exit");
}
